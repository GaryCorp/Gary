"""GaryCorp specialist team: Susan (Research & Strategy), Dave (Security),
Linda (Operations), Catherine (Finance), and Lauren (Ethics), managed by Gary.

    roster.py    identity, prompts, permissions, limits (the authority)
    models.py    agent definitions and structured reports
    gateway.py   the only path from an agent to company data, with checks
    context.py   least-privilege context packages
    ease.py      client for the EASE ethical decision-making service (Lauren)
    executor.py  the boundary to the agent framework
    crew.py      the CrewAI executor
    runner.py    one execution path for every assignment
    service.py   delegation, management reviews, and reading reports
"""
